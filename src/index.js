import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// 1.Navbar static(position) ----------
// 2.Submit in form send to email(biz.dep@wordmap.uz) and top on footer
// 3.Mobile responsive
// 4.Call phone in footer and remove wordmap.uz
// 5.background-color: #b1e6f8; ----
// 6.Change address name
// 7.Chemical, Technology, Utility(Chage images) + Banner image + About image
// 8.Quality +2 Fonts
// 9.Date 2023+ ---------
// 10.Footer dan email olib tawamsz ----